name: Social Copy Conversion Gate

on:
  workflow_dispatch:

jobs:
  focused-tests:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary gate disabled after successful validation"
