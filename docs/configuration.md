# Configuração

Use [Variáveis de ambiente atuais](ambiente.md). O contrato de desenvolvimento é `.env.example`; produção injeta variáveis no provedor de execução. Não usar tabelas antigas para adicionar providers ou integrações não lidos pelo código atual.
